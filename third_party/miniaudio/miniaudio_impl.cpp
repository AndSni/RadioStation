/* Single translation unit providing miniaudio's implementation. Every other
 * file includes the bare miniaudio.h header (declarations only); this is the
 * one place MINIAUDIO_IMPLEMENTATION is defined, per miniaudio's documented
 * single-header integration pattern. */
#define MINIAUDIO_IMPLEMENTATION
#include "miniaudio.h"
